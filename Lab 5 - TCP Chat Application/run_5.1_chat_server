#!/usr/bin/env bash
# Compiles and runs 5.1_chat_server.c directly. Usage: ./run_5.1_chat_server [args...]
set -euo pipefail
dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
src="$dir/5.1_chat_server.c"
bin="$(mktemp)"
gcc -std=c17 -O2 -Wall -Wextra -Wpedantic -o "$bin" "$src"
exec "$bin" "$@"
