#!/usr/bin/env bash
# Compiles and runs 4.6_reverse_string_server.c (compact version) directly. Usage: ./run_4.6_reverse_string_server [args...]
set -euo pipefail
dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
src="$dir/4.6_reverse_string_server.c"
bin="$(mktemp)"
gcc -std=c17 -O2 -Wall -Wextra -Wpedantic -o "$bin" "$src"
exec "$bin" "$@"
